namespace basil
{
  // token name number pair, used to provide operator (non-keyword) token numbers to FSM at load time
  struct TokenNumber
  {
    char const * name;
    int num;
  };
  typedef TokenNumber const * TokenNumberPtr;
}
